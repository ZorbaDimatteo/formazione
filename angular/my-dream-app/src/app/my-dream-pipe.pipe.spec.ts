import { MyDreamPipePipe } from './my-dream-pipe.pipe';

describe('MyDreamPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MyDreamPipePipe();
    expect(pipe).toBeTruthy();
  });
});
